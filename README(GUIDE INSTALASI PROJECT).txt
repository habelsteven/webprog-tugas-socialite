1. jalankan XAMPP
2. buka file 
3. ubah CLIENT_ID DAN CLIENT_SECRET pada file .env sesuai dengan sistem auth yang digunakan
4. jalankan terminal "php artisan migrate:fresh"
5. jalankan terminal "php artisan serve"
6. pada browser anda tambahkan "/login" pada alamat yang ada pada search bar
